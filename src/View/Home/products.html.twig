{% extends 'layout.html.twig' %}

{% block content %}
	<h1>List of Products</h1>
	<div class="container">

		{% for key, product in products %}
			{% if (key)%3 == 0  %}
				<br>
			{% endif %}
			{{product}}
		{% endfor %}

	</div>
	{{ dump(products) }}
{% endblock %}
